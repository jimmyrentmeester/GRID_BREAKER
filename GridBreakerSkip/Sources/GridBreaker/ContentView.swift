import SwiftUI

/// App entry → the menu hub + router (see RootView). NeonTheme + neonGlow live in
/// NeonTheme.swift (ported from iOS, palette-driven for cosmetic parity).
struct ContentView: View {
    var body: some View {
        RootView()
    }
}
